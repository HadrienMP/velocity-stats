web: ./build/install/velocity-stats/bin/velocity-stats
    port=$PORT
    pivotalProjectId=$pivotalProjectId
    pivotalToken=$pivotalToken
    jira.user=$jira.user
    jira.password=$jira.password
    jira.host=$jira.host
    jira.project=$jira.project
    jira.jql.customization=$jira.jql.customization